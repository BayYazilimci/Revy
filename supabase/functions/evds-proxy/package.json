{
  "name": "evds-proxy",
  "version": "1.0.0",
  "description": "TCMB EVDS API proxy for housing price index data",
  "main": "index.ts",
  "scripts": {
    "start": "deno run --allow-net --allow-env index.ts"
  }
}
