{
  "name": "generate-pdf",
  "version": "1.0.0",
  "description": "Generate PDF from property listing",
  "main": "index.ts",
  "scripts": {
    "start": "deno run --allow-net --allow-env index.ts"
  }
}
