{
  "name": "ai-assistant",
  "version": "1.0.0",
  "description": "Claude API proxy for AI assistant",
  "main": "index.ts",
  "scripts": {
    "start": "deno run --allow-net --allow-env index.ts"
  }
}
